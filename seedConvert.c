#include <string.h>

int seedConvert ( char *line ) { /*names function*/
    int i = 0; /*counter*/
    int l = 0; /*stores ansi value of character for comparision purposes*/
    int numberOfConversions = 0; /*keeps track of number of conversions*/
    
    for ( i = 0; i < strlen(line) ; i++) /*check all characters in the line*/
    {   
        
    }
    return (numberOfConversions); /*return number of characters converted*/
}